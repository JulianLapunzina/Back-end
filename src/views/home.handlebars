{{!-- <div>
    <h1>Products</h1>
    {{#each products}}
        <div class="products-container">
            <h2>Título: "{{this.title}}"</h2>
            <p>Descripción: "{{this.description}}"</p>
            <p>Precio: "{{this.price}}"
            Código:"{{this.code}}"
            Stock: "{{this.stock}}"
            </p>
        </div>
    {{/each}}
    --}}


<script src="/socket.io/socket.io.js"></script>
<script src="/js/home.js"></script>
